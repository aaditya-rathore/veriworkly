# Executive Management Family

This folder is reserved for future executive-management-focused resume templates.

Planned conventions:

- Keep per-template folders under `templates/`.
- Keep family-level shared renderers under `shared/`.
- Keep each template `index.tsx` focused on orchestration and section components.
