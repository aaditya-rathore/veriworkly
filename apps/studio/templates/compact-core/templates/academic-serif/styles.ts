export const academicSerifStyles = {
  wrapper:
    "mx-auto max-w-[8.5in] border border-[var(--resume-border)] bg-[var(--resume-page-bg)] p-[max(32px,var(--page-padding))] font-serif text-[var(--resume-text)]",
  section: "mb-4",
  sectionTitle:
    "mb-1 border-b border-[var(--resume-border)] pb-0.5 text-[11pt] leading-[var(--resume-heading-leading)] font-bold tracking-[0.1em] uppercase text-[var(--resume-section-heading)]",
  bodyText: "text-[9.5pt] leading-[var(--resume-body-leading)] text-[var(--resume-muted)]",
} as const;
