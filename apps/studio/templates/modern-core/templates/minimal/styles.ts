export const minimalStyles = {
  wrapper:
    "rounded-[2rem] border border-[var(--resume-border)] bg-[var(--resume-page-bg)] p-[var(--page-padding)] text-[var(--resume-text)]",
  stack: "space-y-[var(--section-gap)]",
  mutedBlock: "rounded-3xl border border-[var(--resume-border)] bg-[var(--resume-section-bg)] p-5",
} as const;
